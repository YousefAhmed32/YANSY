# Dependencies
node_modules

# Logs (PM2, etc.)
logs
*.log

# Environment
.env
.env.local
.env.production

# OS / Editor
.DS_Store
.idea
*.local

# Design/media masters pulled out of client/public so they stop shipping in the
# build. Kept on disk for reference; the originals remain in git history.
design-assets/

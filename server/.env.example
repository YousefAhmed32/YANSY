# Server Environment Variables
# Copy this file to .env and fill in your actual values
# NEVER commit .env to version control

# Server Configuration
PORT=5000
NODE_ENV=production

# Database (use MONGODB_URI; MONGO_URI is supported for backward compatibility)
MONGODB_URI=mongodb://localhost:27017/yansy
# MongoDB Atlas: mongodb+srv://USER:PASS@cluster.mongodb.net/yansy?retryWrites=true&w=majority

# JWT Authentication (generate strong secret: openssl rand -base64 32)
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
JWT_EXPIRES_IN=7d

# Client URL for CORS and Socket.io (comma-separated for multiple origins)
# Production: https://yansytech.com
# Dev + Prod: https://yansytech.com,http://localhost:5173,http://127.0.0.1:5173
CLIENT_URL=https://yansytech.com,http://localhost:5173,http://127.0.0.1:5173

# Optional: Cloud Storage Provider
# CLOUD_PROVIDER=cloudinary
# CLOUDINARY_CLOUD_NAME=your-cloud-name
# CLOUDINARY_API_KEY=your-api-key
# CLOUDINARY_API_SECRET=your-api-secret
